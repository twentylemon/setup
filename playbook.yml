---
- hosts: localhost
  module_defaults:
    ansible.builtin.shell:
      executable: /bin/bash
  roles:
    - { role: bash, tags: bash }
    - { role: git, tags: git }
    - { role: spark, tags: spark }
    - { role: sdk, tags: sdk }
