---
- hosts: localhost
  vars:
    home: "{{ ansible_env.HOME }}"
  roles:
    - { role: bash, tags: bash }
    - { role: git, tags: git }
    - { role: sdk, tags: sdk }
    - { role: claude, tags: claude }
