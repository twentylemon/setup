# Global Preferences

## Git
- Feature branches should always use the prefix `twentylemon/` (e.g. `twentylemon/feature-name`)

## MCP Servers
- If an MCP server fails or returns auth errors, do not try to work around it. Stop and tell me so I can reauthenticate.
